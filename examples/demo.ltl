// examples/demo.ltl — runnable demo for ltl_cgroup_read

import std::io
use ltl_cgroup_read as lib

fn main() {
    io::println("ltl_cgroup_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}